{
  "name": "@repo/core",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "check:types": "bun run --bun tsc",
    "test:types": "bun run --bun tsc -p tsconfig.types.json --diagnostics",
    "test": "bun test:types && bun test"
  },
  "dependencies": {},
  "devDependencies": {
    "@repo/pack-utils": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "expect-type": "^1.3.0",
    "typescript": "catalog:",
    "zod": "catalog:"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "imports": {
    "#*": "./src/*"
  }
}
