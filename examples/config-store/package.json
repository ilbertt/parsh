{
  "name": "@repo/examples-config-store",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "start": "bun run build > /dev/null && bun dist/main.js",
    "check:types": "bun run --bun tsc",
    "generate": "parsh-codegen generate --commands src/commands --out src/commandTree.gen.ts"
  },
  "dependencies": {
    "@parshjs/core": "../../packages/core/pkg",
    "@parshjs/files": "../../packages/files/pkg",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@parshjs/codegen": "../../packages/codegen/pkg",
    "@repo/typescript-config": "workspace:*",
    "typescript": "catalog:"
  }
}
