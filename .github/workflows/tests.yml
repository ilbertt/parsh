name: tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    name: test:required
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      - uses: ./.github/actions/prepare

      - name: Test
        run: bun run test
