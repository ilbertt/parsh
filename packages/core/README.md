# @parsh/core

Type-safe router for TypeScript CLIs. Filesystem-driven command tree, Standard Schema v1 for validation, end-to-end inference through a generated `CommandRegistry` augmentation.

Internal workspace package `@repo/core` builds into `pkg/`, which is published to npm as `@parsh/core`.
